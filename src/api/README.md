
# API

Minimal API scaffold will be created by `scripts/bootstrap.ps1`.

## After scaffold
- Configure ProblemDetails and global exception handler.
- Register typed HttpClient with resiliency handlers.
- Enable OpenTelemetry instrumentation.
